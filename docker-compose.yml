services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: igsearch
      POSTGRES_PASSWORD: igsearch
      POSTGRES_DB: igsearch
    ports:
      - "5432:5432"
    volumes:
      - igsearch_data:/var/lib/postgresql/data

volumes:
  igsearch_data:
